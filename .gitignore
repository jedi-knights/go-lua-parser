# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
dist/

# Test binaries
*.test

# Coverage output
*.out
coverage.out
coverage.html
coverage.xml
coverage/
htmlcov/

# Go profiling artifacts (from `go test -cpuprofile=...` / -memprofile / -blockprofile)
*.prof
*.pprof

# Test result artifacts (CI-generated)
test-results.json

# Go workspace
go.work
go.work.sum

# Environment / local secrets — never commit
.env
.env.*
!.env.example

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Claude Code workspace directory (per-session state, not portable)
.claude/

# OS metadata
.DS_Store
Thumbs.db
